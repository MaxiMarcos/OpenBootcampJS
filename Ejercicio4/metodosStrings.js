let nombre = "Maxi ";

let apellido = "Marcos";

let estudiante = nombre.concat(apellido);

let mayusEst = estudiante.toUpperCase();
let minusEst = estudiante.toLowerCase();

let numLetras = estudiante.length;

let letraNombre = nombre[0];

let finalApellido = apellido.slice(-1);

let sinEspacio = estudiante.replace(/ /g, "");

let estudianteBool = estudiante.includes(nombre);